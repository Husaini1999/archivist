node_modules/
dist/
.env
data/*.db
data/*.db-*
coverage/
*.log

# Personal project memory must never land in the public product repo.
# Keep memory/projects/.gitkeep tracked. A private ARCHIVIST_HOME git repo stores real history.
memory/projects/*/

# Personal Cursor rules
.cursor/

